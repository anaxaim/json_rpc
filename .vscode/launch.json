{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Greet Service",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "${workspaceFolder}/server",
            "cwd": "${workspaceFolder}"
        }
    ]
}